# Changelog

Dates and what shipped. No operational detail, no wallets, no balances.

## 2026-09-17

- REAL_HOLDERS is live on the public bag room. Trading remains PAPER.
- Holder-voted exits: one wallet, one vote, no quorum; a tie is EXIT.
- Test autoexit is off; a bag closes only on real-holder EXIT votes.
- Public snapshot of the code and docs (this repository).

## 2026-09-16

- Public site at [flyfam.xyz](https://flyfam.xyz) and the brand kit.
- Official community links: Telegram and X.
- Autoexit v2 for PAPER / test-holder fallback only: bands at ±20% **gross**
  return, 600 s of market silence, 3600 s maximum hold. Net is displayed and
  decides nothing.
- Launch tooling prepared and left inert. Real money is not live.
- Creator-fee split: facts recorded, worker not written.

## 2026-09-13

- Frozen product brain `brains/trader-v1` (clean reference, every plastic
  KC→MBON gain at 1.0). Learning is off in the product loop.
- Continuous Pons paper loop and the spectacle feed contract
  (`docs/SPECTACLE_FEED.md`).
- D11-001 (first neural run on the repaired Pons environment) closed
  **INCONCLUSIVE** by its own registered floor (15 of 20 LEARNING episodes).
- D12-001 (school mode, relative cohort reinforcement) closed **INCONCLUSIVE**
  by two of its own conditions (coverage, weight saturation).

## 2026-09-12

- D10: Pons v2 memecoin environment — integer-exact curve port, replay paper
  run and a live paper hour. Canonical product pivot off IBM/Kibot live.
- D11 design wave (no neural run): admission v2 (recent activity), sensory
  context v2 (`since_last_trade`, trade count, gross volume), reinforcement
  scale fitted so outcome size teaches again.

## 2026-09-11

- Phase Zero audit of vendored flycoinrh (`upstream/` at `5aab4e78`, MIT).
  mushroom-body dopaminergic read is transposed (MBON→DAN feedback).
- Connectome downloaded (MaleCNS v1.0, CC-BY). Three matrices, one index,
  `[post, pre]`. Plasticity fix in our code; upstream untouched.
- Phase One: MarketSnapshot, SensoryEncoder, BrainRunner, ActionDecoder,
  paper execution, DecisionRecord. Decoder pre-registered before any PnL.
- k = 8 readout integration. θ₈ measured at the neutral reference.
- Historical IBM/OIH path and the local read-only observer.
- D7: WARMUP-selected horizon; context ranking at chance in both branches
  (conclusion B).
- D8: retrospective input diagnostic — no detectable signal with the
  specified methods on the tested periods. Alignment audit: D7 taught 1–19
  minute holds and scored 90-minute ones.
- D9(a): viewer product surface over recorded events.
- D9(b): target-aligned fixed-hold experiment. No improvement detected on
  those periods.

Nothing in this changelog is a claim of skill, alpha or profitability.
